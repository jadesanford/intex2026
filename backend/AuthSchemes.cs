namespace OpenArms.Api;

/// <summary>Short-lived cookie scheme used only between Google OAuth callback and issuing a JWT.</summary>
public static class AuthSchemes
{
    public const string ExternalCookie = "OpenArms.ExternalOAuth";
}
